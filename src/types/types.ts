export type Truck = {
    RequestDate: string,
    TrailerID: string,
    SCAC: string,
    Plant: string,
    ScheduledDate: string,
    ScheduledTime: string,
    Door: string,
    ArrivalTime: string,
}
